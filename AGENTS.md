# Agent Instructions

1. 每次新会话开始处理本项目之前，先阅读 `PROJECT_STRUCTURE.md`。
2. 每次修改项目后，检查是否需要同步更新 `PROJECT_STRUCTURE.md`。如果改动影响项目结构、启动流程、配置、构建发布、WebView 消息、核心模块职责或测试结构，必须更新。
3. 每次修改完成后，都要先构建一次，再给出最终结果。
4. 每次构建前，先自动查找并关闭正在运行的 `WuGesture`，避免文件锁定。
