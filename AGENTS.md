# Agent Instructions

1. 用户有疑问时，必须先回答疑问，然后再询问用户是否继续下一步。
2. 完成任务尽量使用子代理，而非直接在对话中完成。
3. 每次新会话开始处理本项目之前，先阅读 `PROJECT_STRUCTURE.md`。
4. 每次修改项目后，检查是否需要同步更新 `PROJECT_STRUCTURE.md`。如果改动影响项目结构、启动流程、配置、构建发布、WebView 消息、核心模块职责或测试结构，必须更新。
